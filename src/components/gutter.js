const h = require('inferno-hyperscript')

module.exports = Gutter

function Gutter ({  }) {
  const firstBytes = []

  // TODO Actually implement showing starting bytes

  return h('div', firstBytes)
}
